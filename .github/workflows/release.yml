# .github/workflows/release.yml
name: Build and Release Stable

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    if: contains(toJson(github.event.head_commit.message), 'stable release')
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          # ---------- Linux (CGO ON + DuckDB tag) ----------
          - { goos: linux,  goarch: amd64, goversion: "1.23", runner: "ubuntu-latest",  enable_cgo: true,  use_zig: true,  duckdb_tag: true }
          - { goos: linux,  goarch: arm64, goversion: "1.23", runner: "ubuntu-latest",  enable_cgo: true,  use_zig: true,  duckdb_tag: true }

          # Доп. цель без CGO (32-bit) — без DuckDB
          - { goos: linux,  goarch: 386,   goversion: "1.23", runner: "ubuntu-latest",  enable_cgo: false, use_zig: false, duckdb_tag: false }

          # ---------- macOS (CGO ON + DuckDB tag) ----------
          - { goos: darwin, goarch: amd64, goversion: "1.23", runner: "macos-13",      enable_cgo: true,  use_zig: false, duckdb_tag: true }
          - { goos: darwin, goarch: arm64, goversion: "1.23", runner: "macos-14",      enable_cgo: true,  use_zig: false, duckdb_tag: true }

          # ---------- *BSD (CGO OFF, без DuckDB) ----------
          - { goos: freebsd, goarch: amd64, goversion: "1.23", runner: "ubuntu-latest", enable_cgo: false, use_zig: false, duckdb_tag: false }
          - { goos: openbsd, goarch: amd64, goversion: "1.23", runner: "ubuntu-latest", enable_cgo: false, use_zig: false, duckdb_tag: false }
          - { goos: netbsd,  goarch: amd64, goversion: "1.23", runner: "ubuntu-latest", enable_cgo: false, use_zig: false, duckdb_tag: false }

          # ---------- Windows (всегда без DuckDB) ----------
          - { goos: windows, goarch: amd64, goversion: "1.23", runner: "windows-latest", enable_cgo: false, use_zig: false, duckdb_tag: false }
          - { goos: windows, goarch: arm64, goversion: "1.23", runner: "windows-latest", enable_cgo: false, use_zig: false, duckdb_tag: false }

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "${{ matrix.goversion }}"
          cache: true
          cache-dependency-path: go.sum

      # ===== Linux: Zig для CGO без apt-get =====
      - name: Setup Zig (for Linux CGO)
        if: matrix.use_zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          zig-version: "0.12.0"

      - name: Tidy deps (POSIX)
        if: runner.os != 'Windows'
        shell: bash
        run: go mod tidy

      - name: Tidy deps (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: go mod tidy

      - name: Print toolchain (POSIX)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          echo "GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} CGO=${{ matrix.enable_cgo }} TAG_DUCKDB=${{ matrix.duckdb_tag }}"
          go env
          if command -v zig >/dev/null 2>&1; then zig version || true; fi

      - name: Print toolchain (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Write-Host "GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} CGO=${{ matrix.enable_cgo }} TAG_DUCKDB=${{ matrix.duckdb_tag }}"
          go env

      - name: Build (POSIX)
        if: runner.os != 'Windows'
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          set -euo pipefail
          VERSION=${{ github.run_number }}
          echo "Building version $VERSION for $GOOS/$GOARCH"

          # CGO toggle
          if [ "${{ matrix.enable_cgo }}" = "true" ]; then
            export CGO_ENABLED=1
          else
            export CGO_ENABLED=0
          fi

          # Linux CGO через zig cc
          if [ "$GOOS" = "linux" ] && [ "$CGO_ENABLED" = "1" ]; then
            if [ "$GOARCH" = "amd64" ]; then
              export CC="zig cc -target x86_64-linux-gnu"
            elif [ "$GOARCH" = "arm64" ]; then
              export CC="zig cc -target aarch64-linux-gnu"
            fi
          fi

          # Теги сборки
          TAGS="netgo,osusergo"
          if [ "${{ matrix.duckdb_tag }}" = "true" ]; then
            TAGS="$TAGS,duckdb"
          fi

          mkdir -p dist
          BIN_NAME="chicha-isotope-map_${GOOS}_${GOARCH}"
          [ "$GOOS" = "windows" ] && BIN_NAME="${BIN_NAME}.exe"

          GOFLAGS="-trimpath -buildvcs=false" \
          go build -tags "$TAGS" \
                   -ldflags "-s -w -X main.CompileVersion=$VERSION" \
                   -o "dist/${BIN_NAME}"

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          $ErrorActionPreference = "Stop"
          $VERSION = "${{ github.run_number }}"
          Write-Host "Building version $VERSION for $env:GOOS/$env:GOARCH"

          # Windows: всегда без CGO и без DuckDB
          $env:CGO_ENABLED = "0"
          $tags = "netgo,osusergo"

          New-Item -Force -ItemType Directory dist | Out-Null
          $bin = "chicha-isotope-map_${{ matrix.goos }}_${{ matrix.goarch }}.exe"

          go build -tags $tags `
                   -ldflags "-s -w -X main.CompileVersion=$VERSION" `
                   -o "dist/$bin"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bin-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist-raw/

      - name: Publish GitHub Release (tag latest)
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: latest
          name: "Latest Stable Release"
          prerelease: false
          files: dist-raw/**/*

